extend type Query {
    # 페이지별로 user 항목을 가져와요.
    # fetch user items by pagination
    menus: [Menu]
}

# user 항목의 구조
# structure of user item
type Menu {
    id: ID!
    name: String
    nameEn: String
    uri: String
    description: String
    children: [Menu]
}
